# DISCLAIMER: This file is generated automatically. Please do not modify. Manual changes are not supported.

# When set to TRUE turns off AutoUpdate mechanism
#nobootstrap TRUE

# The base URL of the Collector/Server this agent should connect to. 
#Server http://localhost:8020

# The address of proxy server
# Default: empty
#Proxy

# Tenant value
# Default: empty
#Tenant

# The loglevel, valid values are: [0..8] or [finest|finer|fine|config|info|warning|severe|debug|none]
# Default: info
Loglevel info

# The console loglevel, valid values are: [0..8] or [finest|finer|fine|config|info|warning|severe|debug|none].
# Depending on this setting, log messages will also be written to the console (or to the 'stderr' output).
# Default: none
ConsoleLoglevel none

# Names of processes executables that should be monitored, besides recognized DBs, Java apps etc.
#
# names are case insensitive
# names should be separated by commas
CustomMonitoredApp CPULoad,CPULoad64,CPULoad.exe,CPULoad64.exe,cpuload*

# Maximum amount of memory that may be allocated by protobuf messages
BufferSize 400mib

# Maximum size of messages queued in native dispatcher  
asyncmessagequeuesize 400mib
